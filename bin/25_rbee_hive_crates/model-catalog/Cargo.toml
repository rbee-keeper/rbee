[package]
name = "rbee-hive-model-catalog"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dirs = "5.0"
chrono = { version = "0.4", features = ["serde"] }

# TEAM-402: Artifact types contract
artifacts-contract = { path = "../../97_contracts/artifacts-contract" }

# Shared catalog abstraction
rbee-hive-artifact-catalog = { path = "../artifact-catalog" }

[dev-dependencies]
tempfile = "3.8"
