# TEAM-135: Created by TEAM-135 (scaffolding)

[package]
name = "rbee-hive-download-tracker"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
name = "rbee_hive_download_tracker"
path = "src/lib.rs"

[dependencies]
# TEAM-385: Added narration for progress updates
observability-narration-core = { path = "../../99_shared_crates/narration-core" }
stdext = "0.3"  # Required for n!() macro (function_name!())

[dev-dependencies]
# Add dev dependencies as needed
